@model Clientes
@{ Layout = "/Views/Shared/Layout.cshtml"; }

@* <nav aria-label="breadcrumb">
  <ol class="breadcrumb">
    <li class="breadcrumb-item"><a href="/clientes/index">Clientes</a></li>
    <li class="breadcrumb-item active" aria-current="page">Update</li>
  </ol>
</nav>

<form action="/clientes/update/@Model.IdCliente" method="POST">
    <input name="Nome" class="form-control" value="@Model.Nome" />
    <input name="Sobrenome" class="form-control" value="@Model.Sobrenome"/>
    <input name="CPF" class="form-control" value="@Model.CPF"/>
    <input name="Telefone" class="form-control" value="@Model.Telefone"/>
    <br/>
    <button class="btn btn-primary">Atualizar</button>
</form> *@

<style>
  .modal-body {
  margin-top: 22vw;
  left:50%;
  align-items:center;
  transform: translate(-50%, -50%);
  width: 40vw ;
  background-color: #fff;
  padding: 1.2rem;
  border-radius: 0.5rem;
  z-index: 10;
  margin-bottom: 5vw; 
}
  .modal-body input{
  margin-bottom:0.6vw;
  border: none;
  border: 0.01vw solid;
  border-radius: 0.7vw;
  padding: 0.6vw;
}
  .modal-body input {

  font-size: 1.1vw;
  font-family: Arial, Helvetica, sans-serif;
  margin-bottom: 0.5vw;
  font-weight: bold;
}
.footer{
  position: fixed;
}
</style>

<body>
        <div class="modal-body">
          <form action="/clientes/update/@Model.IdCliente" method="post">
          <input name="Nome" class="form-control" value="@Model.Nome" />
          <input name="Sobrenome" class="form-control" value="@Model.Sobrenome"/>
          <input name="CPF" class="form-control" value="@Model.CPF"/>
          <input name="Telefone" class="form-control" value="@Model.Telefone"/>
          <br/>
          <button class="btn btn-primary">Atualizar</button>
          </form>
        </div>
</body>